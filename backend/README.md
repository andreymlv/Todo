# Quick Start

Requires:

- Docker
- Docker Compose
- C compiler
- PostgreSQL (for libpq)

To run application:

```console
docker-compose up -d
./build.sh
./todo
```

To stop postgres container

```console
docker-compose down
```

